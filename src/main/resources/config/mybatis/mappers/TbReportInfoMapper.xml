<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.silita.dao.TbReportInfoMapper">


    <select id="queryRepTitle" resultType="java.lang.String" parameterType="map">
        select rep_title from mishu.`tb_report_info` where order_no = #{orderNo}
    </select>

</mapper>